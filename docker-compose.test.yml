name: headless-backend-test

services:
  test:
    build:
      context: .
      target: build
    command: go test -race -count=1 -p 2 ./...
    environment:
      CGO_ENABLED: "1"
      GOMAXPROCS: "2"
    cpus: 2
    mem_limit: 4g
