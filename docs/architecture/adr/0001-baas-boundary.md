# ADR-0001: Codex is a BaaS, not a site module

- Status: Accepted

GoBackend serves generic content API/storage/auth semantics. Consumers use a
published pinned binary/module and own their BFF, UX, manifest and content.
